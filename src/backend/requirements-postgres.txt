# Optional PostgreSQL async driver dependency (`pip install -r requirements-postgres.txt`)
# Use this when attaching an external or managed cloud PostgreSQL database (`DATABASE_URL=postgresql+asyncpg://...`).
asyncpg>=0.29.0
