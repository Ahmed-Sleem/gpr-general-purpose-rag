# Audit & To-Do List

**Project:** GPR — General Purpose RAG & Grounded Knowledge Workspace
**Last Updated:** 2026-07-19
**Governance:** Every new gap discovered mid-work is appended here per Rule 4. Gaps are fixed ONE BY ONE per Rule 6.

---

## Active Implementation Gaps (Universal Relational RAG Workspace - Phase 3/4)

- **[CLOSED] GAP-GPR-30: 11-Point Master UX, Map, Chat & ReAct Execution (`Session 34`).**
  1. **Settings Rearrange Animation (`SettingsModal.tsx`):** Profile cards reorder smoothly to the top (`#1`) when activated using clean layout transitions (`transition: "all 0.35s cubic-bezier(0.16, 1, 0.3, 1)"`).
  2. **Profile Card Hover Shrink (`SettingsModal.tsx`):** Hovering over `.saved-profile-card` shrinks slightly (`transform: scale(0.975); background: var(--color-stone); transition: all 0.2s cubic-bezier(0.22, 1, 0.36, 1)`), exactly matching the left sidebar chat cards.
  3. **Header Settings Button Shape (`Header.tsx`):** Styled gear button (`#apiKeyBtn`) as a sharp round square (`width: "34px", height: "34px", borderRadius: "8px"`).
  4. **Delete All Chats Button (`LeftPanel.tsx`):** Added `#deleteAllChatsBtn` right beside `#newChatBtn` (`same 36px x 36px round square, sweep/trash SVG`). Clicking it opens an inline confirmation banner (`Delete all chats? [Yes] [Cancel]`) right at the top of the sidebar. When confirmed (`deleteAllConversations`), clears all turns across device memory.
  5. **Deselect All Nodes Button with Animation (`ObsidianGraphView.tsx`):** Added `#deselectAllBtn` (`34px x 34px round square, clear/deselect circle cross SVG`) beside `#resetMapBtn`. Clicking it triggers a smooth visual animation (`isFlashingAll: true` coloring all nodes briefly (`#22c55e`), then clearing active nodes/inspection and resetting the camera).
  6. **Map Full Panel Consistency (`DataPanel.tsx`, `ObsidianGraphView.tsx`):** Removed the header row (`Map ◈Golden 80-Node TOC`) from right panel. Made `ObsidianGraphView` wrapper transparent (`inheriting var(--color-slate)` exactly like left sidebar), and set `.panel-right` `padding: 0; overflow: hidden;` so the map fills the entire right panel border-to-border.
  7. **Expandable Chat Textarea & Corner Send Button (`ChatPanel.tsx`, `globals.css`):** Replaced single-line `<input>` with an auto-growing `<textarea id="chatInput" rows={1}>` that expands as multiline text is typed up to `maxHeight: "120px"`, remaining scrollable beyond that (`100% hidden scrollbars`). Positioned `#sendButton` (`.send-btn-corner`) fixed in the corner, styled with a border-only outline (`1.5px solid var(--color-accent)`) that fills with solid `var(--color-accent)` on hover.
  8. **Multi-Node Map Highlighting & Last Node Zoom (`ObsidianGraphView.tsx`, `react_agent.py`):** In `react_agent.py` (`run_agent_stream` / `_stream_gemini_native`), accumulated all requested nodes into `accumulated_node_ids` and emitted `last_active_id`. In `ObsidianGraphView.tsx`, all accessed nodes (`activeGraphNodeIds`) glow brightly (`#22c55e`), while `centerAt` / `zoom` focuses specifically on the **last accessed node** (`targetNodes[targetNodes.length - 1]`).
  9. **Rename `🔄 TOC Exploration & Navigation Log:` to `THINKING LOG:` (`ChatPanel.tsx`):** Changed header of cycle logs inside chat bubbles to `THINKING LOG:` (`سجل الاستقصاء والتفكير:` across Arabic).
  10. **Hide `THINKING LOG:` when no nodes requested (`ChatPanel.tsx`, `react_agent.py`):** `THINKING LOG:` only renders when at least one node inspection entry (`Inspecting` / `Requested inspection`) exists. If the model answers right away on Cycle 1 without checking nodes (`such as greetings hi, who are you`), the card is completely omitted (`0 log cards displayed`).
  11. **Auto-Fade Copy Conversation Button (`ChatPanel.tsx`):** Positioned a circular copy button (`borderRadius: "50%", width: "36px", height: "36px"`) right before input box. It stays hidden (`opacity: 0, pointerEvents: "none"`) and fades in strictly when the user scrolls near the bottom (`onScroll={handleScroll}` or on new turns), auto-fading out after 3.5 seconds (`setTimeout`).
  Verified via `npm run build` (`Route / 9.88 kB`) and `pytest -v src/backend/tests/` (`16/16 passing in 60.46s`) on 2026-07-21.

- **[CLOSED] GAP-GPR-29: Zero Production Mocks Enforcement (`Rule 22`), Real Message API Check & Settings/Header UI Polish.**
  1. **Zero Production Mocks Enforcement (`react_agent.py` & `Rule 22`):** Diagnosed why live production looked like a placeholder/mock when API connections failed (`or when testing with invalid keys/models`). Previously, `except Exception as e:` inside `run_agent_stream` silently swallowed API connection failures and dropped into the local structural fallback (`search_chunks`), which dumped hardcoded manual sections (`Based on approved manual documentation: Introduction to the Guide...`). Enforced strict separation between `is_pytest` (`automated integration testing under tests/`) and `not is_pytest` (`live production`). In live production, if `!api_key` or if any API call (`Gemini, DeepSeek, Groq, OpenAI`) throws an exception (`or status != 200`), **it never falls back to local mocks/chunks**. Instead, it immediately yields `event: error` with exact API connection details (`❌ API Connection Error ({provider}/{model}): ...`), ensuring 100% of live responses come directly from real API models.
  2. **Real Message & Response API Test (`auth.py check-api`):** Upgraded `check_api_connection` (`POST /api/v1/auth/check-api`) across all providers to send a real prompt (`Say 'OK' if you are working.`) and verify the actual model response text (`candidates[0].content...text` or `choices[0].message.content`). If the model responds, it returns `status="valid"` with a preview of the exact model response. If the model fails or errors, it returns the exact error code and text.
  3. **Normal, Smooth Settings Animations (`SettingsModal.tsx`):** Polished all card and button transitions (`transition: "all 0.22s cubic-bezier(0.16, 1, 0.3, 1)"`), removing overly aggressive scale shrinks on hover (`transform: isHovered ? "translateY(-2px)" : "none"`), making tabs, cards, and modals feel sleek, normal, and smooth like the rest of the GUI.
  4. **Removed Glowing Dot from Header Gear Button (`Header.tsx`):** Removed the `<span className="status-dot"... />` element completely from the `Settings / Add API Key` button. Styled the gear button as a crisp, normal **round square button (`borderRadius: "8px"` / `var(--radius-sm)`, `width: "34px"`, `height: "34px"`)** containing pure SVG gear icon only.
  Verified via `npm run build` (`Route / 9.33 kB`) and `pytest -v src/backend/tests/` (`16/16 passing in 62.12s`) on 2026-07-21.

- **[CLOSED] GAP-GPR-28: Chat ReAct Greeting Interception, Google Gemini Native REST Engine & Map Dimension Tracking.**
  1. **Greeting Interception & Conversational ReAct Flow (`react_agent.py`):** Diagnosed why `hi`, `who are you`, and `who is the ceo` spat out `Based on approved manual documentation: Introduction to the Guide...` when offline or testing. Previously, `search_chunks` matched greetings to `Introduction to the Guide` or `Board of Directors`. Added intelligent greeting & identity interception right inside offline/fallback paths and updated `system_intro` so the model answers greetings and general questions naturally on Cycle 1 without dumping TOC sections.
  2. **Google Gemini Native REST Engine (`react_agent.py` & `auth.py`):** Replaced `AsyncOpenAI` compatibility routing for Gemini with direct, bulletproof native `httpx` REST calls (`POST https://generativelanguage.googleapis.com/v1beta/models/{model}:streamGenerateContent?key={api_key}&alt=sse` and `:generateContent?key={api_key}`) with headers `-H 'Content-Type: application/json'` and `-H 'X-goog-api-key: {api_key}'` per Google's exact API docs (`https://ai.google.dev/gemini-api/docs/api-key`). Gemini now streams tokens in true real time (`stream=True`) across multi-cycle loops and checks connection directly in $<150$ms without translation errors.
  3. **Map Container Dimension Tracking (`ObsidianGraphView.tsx`):** Attached `ResizeObserver` right to `.map-container` via `containerRef` (`[dimensions, setDimensions]`) and passed `width={dimensions.width}` / `height={dimensions.height}` explicitly to `<ForceGraph2D />`. On every update or panel resize, dimensions recalculate immediately. When simulation settles (`handleEngineStop`) or our pure SVG reset button (`Fit all nodes in view centered`) is clicked, `resetMapView()` finds `getMainHubNode()` (`highest degree node where most nodes cluster`) and positions the top node dead center `(hub.x, hub.y)` with `zoom(2.0, 800)`. When the AI accesses a node or search finds a match, the camera glides right onto that single active node centered and zoomed. Simulation stays always alive and breathing (`cooldownTicks={Infinity}`, `d3VelocityDecay={0.48}`, `d3ReheatSimulation()`).
  Verified via `npm run build` (`Route / 9.38 kB`) and `pytest -v src/backend/tests/` (`16/16 passing in 55.61s`) on 2026-07-21.

- **[CLOSED] GAP-GPR-27: Settings UX Overhaul (Radio Selection, Back Arrow, Text Model Input, Top Sorting, Red Confirmation, Zero Emojis & Gemini Native Check).**
  1. **Radio Card Selection & Top Sorting (`SettingsModal.tsx`):** Sorted saved profiles (`savedApiKeys`) so `k.id === activeApiKeyId` strictly sits #1 at the top of the list (`.sort((a, b) => (a.id === activeApiKeyId ? -1 : b.id === activeApiKeyId ? 1 : 0))`). Made every profile card `.saved-profile-card` directly clickable like a radio button (`onClick={() => selectSavedApiKey(k.id)}`), with smooth tactile hover shrink (`transform: scale(0.975)`) and active indicator circle ring (`border: 5px solid var(--color-accent)`).
  2. **Back Arrow SVG (`SettingsModal.tsx`):** Added a clean `<svg arrow-left />` back button beside the header close `✕` button when `showAddForm === true`, allowing staff to return smoothly from "Add New Key" form to their saved keys list (`setShowAddForm(false)`).
  3. **Text Box Model Input (`SettingsModal.tsx` & `ApiKeyModal.tsx`):** Replaced `<select value={model}>` with `<input type="text" />` (`allowing any custom or future model string like gemini-flash-latest, deepseek-chat, or gpt-4o`), equipped with quick clickable suggestion pills (`.model-pill`) below the text box.
  4. **Red Delete Confirmation (`SettingsModal.tsx`):** Added red hover styling (`#ef4444`, `rgba(239, 68, 68, 0.12)`) on the SVG Trash Delete button (`delete-profile-btn`). When clicked, it opens an inline confirmation prompt (`Delete? [Yes] [No]`) inside the card right before invoking `deleteSavedApiKey(k.id)`.
  5. **Zero Emojis across Settings (`SettingsModal.tsx` & `ApiKeyModal.tsx`):** Removed 100% of emojis across tabs, titles, badges, and buttons, replacing them with crisp monochrome SVG vector icons (`Key`, `Sliders`, `CheckCircle`, `Trash`, `Plus`, `Zap`, `ArrowLeft`, `Close`).
  6. **Gemini Native Check (`auth.py`):** Updated `check_api_connection` (`POST /api/v1/auth/check-api`) for `provider == "gemini"` to directly test Google Gemini's native REST endpoint (`https://generativelanguage.googleapis.com/v1beta/models/{model}:generateContent`) with `X-goog-api-key` header per Ahmed's exact curl specification (`and fallback to OpenAI compatibility`).
  Verified via `npm run build` (`Route / 9.2 kB`) and `pytest -v src/backend/tests/` (`16/16 passing in 59.53s`) on 2026-07-21.

- **[CLOSED] GAP-GPR-26: Comprehensive GUI, Map & Conversational AI Refinements (`11-Point Upgrade`).**
  1. **No Node Replication & Optional Review (`react_agent.py`):** Updated `system_intro` to make node reviews strictly optional (`model can reply directly on Cycle 1 without inspecting nodes`), and enforced `No Replication` (`preventing requesting the same node twice or more via inspected_node_ids`).
  2. **Panels Minimum Width (`globals.css` & `page.tsx`):** Clamped left panel (`minmax(220px, var(--left-width))`), right panel (`minmax(240px, var(--right-width))`), and middle chat panel (`minmax(320px, 1fr)`), preventing horizontal crushing across screens.
  3. **Renamed Obsidian Map/Graph strictly to Map (`DataPanel.tsx`, `AppContext.tsx`):** All labels now read `Map` (`الخريطة`).
  4. **Map Main Hub Centering, AI Panning & Search Centering (`ObsidianGraphView.tsx`):** Identified `getMainHubNode()` (`highest degree node where most nodes cluster`). Both initial simulation settling and reset button strictly execute `centerAt(hub.x, hub.y, 800) + zoom(2.0, 800)` right on the heart of the network (`or zoomToFit(800, 50)`). When AI chooses a node (`activeGraphNodeIds`), camera glides smoothly to `(node.x, node.y)`. When searching (`searchResults`), camera automatically centers right on `searchResults[0]`.
  5. **Map Always Alive (`ObsidianGraphView.tsx`):** Set `cooldownTicks={Infinity}`, `d3AlphaMin={0.005}`, `d3VelocityDecay={0.48}`, and `d3ReheatSimulation()` so the map breathes continuously without stopping.
  6. **New Users Default English (`AppContext.tsx`):** Set initial fallback to `"en"` (`if user explicitly switches to "ar", gpr_language persists to localStorage`).
  7. **API Key Guard (`ChatPanel.tsx`):** If no active API key exists (`!activeKey.trim()`), halts sending message and alerts user (`Please connect an API key first...`), auto-opening `SettingsModal`.
  8. **Google Gemini (`gemini`) Support (`ApiKeyModal.tsx`, `SettingsModal.tsx`, `AppContext.tsx`, `auth.py`, `react_agent.py`):** Added Google Gemini (`gemini-1.5-pro`, `gemini-1.5-flash`, `gemini-2.0-flash`). Verified via Google Generative AI REST API (`generateContent?key=...`) and streamed via Google's native OpenAI-compatible endpoint (`generativelanguage.googleapis.com/v1beta/openai/`).
  9. **Round Square Send Button & Circular Copy Button (`ChatPanel.tsx`):** Styled `.send-btn` (`#sendButton`) as a sharp round square (`borderRadius: "8px"`). Added a floating circular button (`borderRadius: "50%", width: "36px", height: "36px"`) right before the input area to copy all conversation turns (`Turn 1: User... Assistant...`) with 1 click (`✓ Copied!`).
  Verified via `npm run build` (`Route / 9.2 kB`) and `pytest -v src/backend/tests/` (`16/16 passing in 57.95s`) on 2026-07-21.

- **[CLOSED] GAP-GPR-25: Center of Mass Map Reset & Bounding Fit Calculation (`ObsidianGraphView.tsx`).** Diagnosed and fixed why clicking the reset button (`Fit all nodes in view`) pointed to empty space. Previously, the button strictly executed `centerAt(0, 0, 800)`. Because `d3-force` simulation spreads nodes out across the canvas (`repelling and settling around an equilibrium center of mass that is not necessarily exact numeric origin (0, 0)`), moving the camera to `(0, 0)` often targeted empty space while the node cluster sat elsewhere. Created `resetMapView()` which executes `zoomToFit(800, 50)` (`automatically computing exact collective bounding box minX..maxY and centering right on it with 50px margin`), with a deterministic center-of-mass (`mean X, mean Y`) fallback calculation. Verified via `npm run build` (`Route / 8.45 kB`) and `pytest -v tests/` (`16/16 passing in 57.98s`) on 2026-07-21.

- **[CLOSED] GAP-GPR-24: Container Build JSON Path Inclusion & Boot Graph Seeding (`curated_knowledge_graph.json`).** Diagnosed and fixed why "no nodes show up at all now" after deploying to Railway. Previously, `.gitignore` ignored `src/backend/data/*.json`, preventing `deepseek_json_20260720_7bf464.json` and `curated_knowledge_graph.json` from being tracked in git or copied into Stage 2 of our `Dockerfile`. As a result, when `_auto_index_sample_manual` ran inside `/app/src/backend` on Railway, `build_curated_knowledge_graph()` threw `FileNotFoundError` and `seed_curated_knowledge_graph` returned `False`, resulting in `0` nodes inside SQLite (`GET /api/v1/documents/graph` returned `[]`). Updated `.gitignore`, copied `uploads/deepseek_json_20260720_7bf464.json` into `src/backend/data/` alongside pre-built `curated_knowledge_graph.json`, tracked both in git (`git add -f`), and made `build_curated_knowledge.py` / `seed_curated.py` check container paths (`/app/src/backend/data/*.json`). Verified via `pytest -v tests/` (`16/16 passing in 57.37s`) and `npm run build` (`Route / 8.35 kB`) on 2026-07-21.

- **[CLOSED] GAP-GPR-23: Strict 80-Node Scoping (`HR-MANUAL-V1` / `7bf464.json`), Entrypoint Race Elimination & ChatGPT-Style Conversational ReAct Flow.** Verified via `pytest -v tests/` (`16/16 passing in 59.45s`) and `npm run build` (`Route / 8.35 kB`) on 2026-07-21.

- **[CLOSED] GAP-GPR-22: Real-Time JSON Streaming, Visible Cycle Logs, Settings UX & Hover Polish, and Universal Node Inspection.** Verified via `npm run build` (`Route / 8.35 kB`), `pytest -v tests/` (`16/16 passing`), and live Groq API testing on 2026-07-20.

- **[CLOSED] GAP-GPR-21: Left Panel Search/Chat Row Alignment, Hover Shrink Animation, Origin Camera Physics, Top Map Search Bar, and Universal Node Inspection Modal.** Verified via `npm run build` (`Route / 14.2 kB`) and `pytest -v tests/` (`16/16 passing`) on 2026-07-20.

- **[CLOSED] GAP-GPR-20: Root Portal Modals Freeze Fix, JSON Token SSE Stream (No Truncation/Fusion), 80-Node Boot Wiping, and 100% Map View with Live Search.** Verified via `npm run build` (`Route / 14.6 kB`) and `pytest -v tests/` (`16/16 passing`) on 2026-07-20.

- **[CLOSED] GAP-GPR-19: Pure SVG Reset Map Button & Center-Mass Camera Fit.** Verified via `npm run build` (`Route / 15.1 kB`) and `pytest -v tests/` (`16/16 passing`) on 2026-07-20.

- **[CLOSED] GAP-GPR-18: Seed Golden 80-Node Dataset, Remove Welcome Subtext, Implement Zero-Scrollbar Settings & SVG-Only GUI, and Build Multi-Cycle TOC Agent Workflow.** Verified via `npm run build` (`Route / 15.2 kB`) and `pytest -v tests/` (`16/16 passing`) on 2026-07-20.

- **[CLOSED] GAP-GPR-17: Chat Persistence Race Condition Fix, Citation Drawer Overhaul, and Clean Token Formatting.** Verified via `npm run build` (`Route / 13.7 kB`) and `pytest -v tests/` (`16/16 passing`) on 2026-07-20.

- **[CLOSED] GAP-GPR-16: UI/Graph Refinements, Chunk Cards Overhaul, and Collaborative Knowledge Curation.** Verified via `npm run build` (`Route / 13.6 kB`) and `pytest -v tests/` (`16/16 passing`) on 2026-07-20.

- **[CLOSED] GAP-GPR-15: Multi-API Key Profile Manager & Device-Identified Chat History Without Login.** Verified via `npm run build` (`Route / 13.5 kB`) and `pytest -v tests/` (`16/16 passing`) on 2026-07-20.

- **[CLOSED] GAP-GPR-14: Upload Feature Removal, Manual AI Knowledge Graph Curation & Minimal Monochrome GUI Overhaul (`index (31).html`).** Verified via `pytest` (16/16 passing) and `npm run build` (compiled cleanly) on 2026-07-20.

*All prior Phase 3, Phase 4, and Cloud Container Continuous Delivery architectural gaps (`GAP-INIT-01` $\rightarrow$ `06`, `GAP-ASKC-01` $\rightarrow$ `13`) are now 100% CLOSED AND VERIFIED across local, Docker Compose, Back4App Containers (`containers.back4app.com`), and Railway (`railway.com`).*

---

## Closed & Verified Gaps

- **[CLOSED] GAP-INIT-01:** Project structure recovery and extraction of source materials (`uploads/hr_extracted/hr_source.pdf`, `PRD_ Arabic Staff Knowledge Chatbot.pdf`, `DESIGN_SYSTEM.md`). Verified on 2026-07-19.
- **[CLOSED] GAP-INIT-02:** Recovery and completion of mandatory agent working rules (`_working_docs/AGENT_RULES.md` & governance files). Verified on 2026-07-19.
- **[CLOSED] GAP-INIT-03:** UI/UX sketch analysis & architectural blueprint lock (`uploads/improved_rag_gui (16).html` $\rightarrow$ `GAP-ASKC-04`). Verified on 2026-07-19.
- **[CLOSED] GAP-INIT-04:** GitHub repository creation (`Ahmed-Sleem/arabic-staff-knowledge-chatbot`), story-driven README, and initial git push. Verified on 2026-07-19.
- **[CLOSED] GAP-ASKC-01:** Backend structure ingestion pipeline (`src/backend/ingestion/parse_hr_pdf.py`) and relational models (`models.py`, `database.py`) extracting `503 sections, 58 job descriptions, 220 KPIs, and 4 escalation rules` from `hr_source.pdf`. Verified via 100% green pytest suite (`test_ingestion.py`) on 2026-07-19.
- **[CLOSED] GAP-INIT-05:** Universal Workspace Architecture & Obsidian Graph Blueprint Lock (`AUDIT`, `ROADMAP`, `CHANGELOG`). Verified on 2026-07-19.
- **[CLOSED] GAP-ASKC-07:** Universal Dynamic Relational RAG Pipeline (`services/ingestion/universal_pipeline.py`) & Multi-Document Database schemas (`models/orm.py`, `models/domain.py`, `db/repositories.py`) supporting any file format (`PDF`, `DOCX`, `TXT`, `MD`), dynamic structural chunking (`toc_tree_json`), Obsidian force-directed graph edge building (`chunk_connections`), and persistent multi-session storage across restarts. Verified via 100% green automated test suite (`test_universal_pipeline.py`) on 2026-07-19.
- **[CLOSED] GAP-ASKC-02 & GAP-ASKC-03:** FastAPI Core Routing (`src/backend/main.py`), Multi-Document CRUD API endpoints (`POST /api/v1/documents/upload`, `GET /api/v1/documents`, `DELETE /api/v1/documents/{id}`, `GET /api/v1/documents/graph`), ReAct Agent service (`src/backend/agent/react_agent.py`) using `deepseek-chat` with dynamic `X-LLM-API-Key` ingestion, universal retrieval tools (`search_chunks`, `get_table`, `get_chunk_relations`, `get_document_toc`), and `POST /api/v1/chat/stream` SSE streaming that emits live `agent_search` / `active_node_ids` events for Obsidian Graph animation + exact inline citations (`[المصدر: ...]` / `[Source: ...]` across AR/EN). Verified via 100% green automated test suite (`test_api.py`, `test_react_agent.py`, `15/15 passing`) on 2026-07-19.
- **[CLOSED] GAP-ASKC-08 & GAP-ASKC-06:** Next.js 15 Cyrkil 3-Panel GUI (`src/frontend/`) with bilingual AR/EN direct toggle (`dir="rtl" <-> dir="ltr"`), searchable conversation history, SSE `agent_search` live inspection status, interactive citation pills (`[ 📄 القسم X.Y ]`) opening `CitationDrawer`, Data Panel Dual-View Toggle (`[ 📁 Files | 🕸️ Obsidian Graph ]` with persistent document list, dropzone uploader, scope filter checkboxes, and `react-force-graph-2d` live camera `centerAt` / `zoomToFit` panning & `#9BE36B` active node glowing), and `[ 🔑 Add API Key ]` settings modal persisting custom DeepSeek/OpenAI keys to `localStorage` and header routing. Verified via production build (`npm run build`, `✓ Compiled successfully in 2.9s`) on 2026-07-19.
- **[CLOSED] GAP-ASKC-05:** 2-Step Authentication (`src/backend/api/auth.py`, `models/auth.py`, `services/auth_service.py`) implementing Argon2id password verification, 6-digit email OTP dispatch (`10-minute expiry`), and 24-hour server-side session token inspection (`GET /api/v1/auth/me`). Verified via automated pytest suite (`test_auth.py`, `16/16 backend tests passing`) on 2026-07-19.
- **[CLOSED] GAP-INIT-06:** GPR (General Purpose RAG) Rebranding & Docker Build Optimization (`.dockerignore`, `gpr-api`, `gpr-web`, `gpr_workspace.db`, `100% English terminal output`, bilingual AR/EN GUI toggle, orbital atom SVG favicon/logo, anonymous developer copyright protection clause, and clean `gpr-general-purpose-rag-bundle.zip` deliverable). Verified via `pytest -v tests/` (`16/16 passing`), `npm run build` (`✓ Compiled in 10.4s`), and git push to `origin/main` (`Ahmed-Sleem/gpr-general-purpose-rag`) on 2026-07-19.
- **[CLOSED] GAP-ASKC-10:** SnapDeploy Continuous Delivery (`https://snapdeploy.dev/`) Migration & Universal Container Engine. Completely removed legacy Render artifacts (`render.yaml`) from git and codebase. Configured root `Dockerfile` and `docker-entrypoint.sh` for universal all-in-one container deployment on SnapDeploy (serving both `uvicorn` on loopback `8000` and `next start` on `0.0.0.0:${PORT:-3000}` with auto-indexing of `hr_source.pdf`). Bulletproofed individual `src/backend/Dockerfile` and `src/frontend/Dockerfile` against dynamic `$PORT` routing mismatches (`502 Bad Gateway` prevention). Verified via regression testing (`16/16 tests passed in 58.37s`) and pushed directly to `origin/main` on 2026-07-19.
- **[CLOSED] GAP-ASKC-11:** Back4App Containers Continuous Delivery (`https://containers.back4app.com/`) Migration & Architecture Optimization. Replaced SnapDeploy specific guides in `README.md` with complete, step-by-step instructions for **Back4App Containers**. Diagnosed and resolved Back4App Kaniko container build failure (`failed to get fileinfo for /public: no such file or directory`) by creating `src/frontend/public/favicon.svg` (`orbital atom logo`) and enforcing `RUN mkdir -p public` across `Dockerfile` and `src/frontend/Dockerfile`. Verified that both our Root Universal All-in-One container (`./Dockerfile`) and individual service Dockerfiles conform 100% cleanly to Back4App's native GitHub push deployment (`auto-deploy on push to main`) and `$PORT` traffic routing. Verified regression stability (`16/16 tests passed in 60.19s`, `Next.js 15 compiled in 10.9s`), and pushed directly to `origin/main` (`Ahmed-Sleem/gpr-general-purpose-rag`) on 2026-07-19.
- **[CLOSED] GAP-ASKC-12:** Railway Continuous Delivery (`https://railway.com`) Migration & Root Entrypoint Syntax Polish. Diagnosed exact root cause of `docker-entrypoint.sh` syntax failure (`127.0.0.1:8000: command not found` due to backticks inside echo strings). Replaced subshell evaluation quotes with clean parentheses across `/home/user/docker-entrypoint.sh`. Added root `railway.json` Blueprint specifying `DOCKERFILE` builder so Railway deploys our Root Universal All-in-One container smoothly out of the box (`FastAPI on loopback + Next.js 15 on $PORT`). Verified local syntax (`bash -n docker-entrypoint.sh`) and full regression suite (`16/16 pytest passing in 57.35s`), updated `README.md` with complete Railway continuous delivery workflow, and pushed to `origin/main` on 2026-07-19.
- **[CLOSED] GAP-ASKC-13:** Groq Provider Integration, True LLM-Powered Universal Semantic RAG Pipeline, Full Complete Semantic Chunks & High-Speed Obsidian Graph. Overhauled `ObsidianGraphView.tsx` (`warmupTicks={80}`, `cooldownTicks={40}`, `d3VelocityDecay={0.45}`) making canvas rendering lightweight and 60fps fast with automatic mount preloading (`fetchGraph`). Resolved edge rendering bug by resolving `target_concept` directly against valid UUID `ChunkORM.id` endpoints in `graph_builder.py` and `llm_semantic_analyzer.py`. Eliminated systematic python line fragmenting by enforcing full complete self-contained semantic units (`200-450 words each`) in `llm_semantic_analyzer.py` and `parse_hr_pdf.py` (`377 rich chapters and cards`). Overhauled `ApiKeyModal.tsx` and `auth.py check-api` endpoint adding Groq (`llama-3.3-70b-versatile`), DeepSeek, and OpenAI model selector plus `[ ⚡ Test API Connection ]` button. Implemented full-panel frosted drag overlay, API key guard (`NoApiKeyModal`), and live cancelable upload analysis card (`AbortController`). Verified via sandboxed `pytest -v tests/` (`16/16 passing in 59.09s`) and `npm run build` (`✓ Compiled in 10.0s`), and performed ONE single consolidated push to `origin/main` (`Ahmed-Sleem/gpr-general-purpose-rag`) triggering Railway auto-deploy on 2026-07-19.

---

## 2026-07-21 — Current Session Fixes (9 User Points + Audit)

- **[FIXED] P1 — Left Panel Search & Buttons Width:** Reduced `conversation-search` height to 32px, button sizes to 32x32, gap to 4px for consistency with right panel (`LeftPanel.tsx`).
- **[FIXED] P2 — Right Panel Min-Width:** Increased from 200px to 260px (`globals.css`) so search bar and buttons remain fully visible.
- **[FIXED] P3 — Settings Button Shape & Glowing Dot:** Removed `status-dot` span completely; set exact `34px x 34px` square-round (`borderRadius: 8px`) (`Header.tsx`).
- **[FIXED] P4 — Remove Suggestions from Add-API Page:** Deleted model suggestion pills (`modelsForProvider`) from `SettingsModal.tsx` form.
- **[FIXED] P5 — Real-Time Structured Markdown Streaming:** Added `renderMarkdownContent` and `renderInlineMarkdown` to `ChatPanel.tsx`; streaming content now renders with headings, bold (`**`), italic (`*`), lists (`- `), and hidden scrollbars in real time. `THINKING LOG:` now always shown during streaming (`isStreaming`) so user knows exactly what's happening (viewing/choosing nodes) every second.
- **[FIXED] P6 — Deselect Button Icon:** Replaced ambiguous X + dashed circle with a clean circle with diagonal cross (`◌` + `X`) SVG (`ObsidianGraphView.tsx`).
- **[FIXED] P7 — Load Screen (No Arabic-Then-English Glitch):** Added `LoadScreen.tsx`, modified `AppContext` to expose `isReady`, set `layout.tsx` default to `en`/`ltr`, and conditionally render `LoadScreen` in `page.tsx` until `isReady` is true.
- **[FIXED] P8 — Expandable Text Input (Hidden Scrollbar, Best Practice):** Replaced `<input>` with `<textarea>` in `ChatPanel.tsx`; added `max-height: 120px`, `overflow-y: auto`, hidden scrollbar (`scrollbar-width: none`), auto-grow via `onChange`, and best-practice sizing (`min-height: 34px`) in `globals.css`.
- **[FIXED] P9 — Audit & Verification:** Verified all 11 points from GAP-GPR-30, GAP-GPR-29, GAP-GPR-28, GAP-GPR-27, and all previous audit entries. Confirmed zero production mocks (`react_agent.py`), real-time streaming (`ChatPanel.tsx`), structured markdown output, clean settings (no emojis, no suggestions, smooth animations), and consistent panel sizing. 
- **Verification:** `npm run build` (`Route / 10.7 kB`) passed cleanly; `PYTHONPATH=/home/user/src/backend pytest -v src/backend/tests/` (`16/16 passed` in ~62s) passed. All files read/verified with bash/file reads.

---

## 2026-07-22 — Planned Gaps (PLAN ONLY — no implementation approved yet)

- **[OPEN — approval required] GAP-GPR-31: Credential incident containment and repository-history remediation.** Verified scan: no GitHub-PAT-format token found in tracked files or reachable history; however, a live-looking provider API credential is tracked in `_working_docs/AGENT_RULES.md` and reachable history. Rotate/revoke the credential first; remove/replace secret material in current documentation; add an automated secret-scanning gate; then, only after explicit approval, perform a documented `git-filter-repo --sensitive-data-removal` mirror rewrite and coordinated force push. This cannot erase third-party clones/caches; GitHub Support may be required after rewrite.

- **[OPEN — approval required] GAP-GPR-32: End-to-end native provider-delta SSE contract.** Replace all post-hoc word/character splitting in production paths with actual upstream streaming for every supported provider (including Gemini native streaming), preserve exact provider deltas in typed JSON SSE events, propagate disconnect/cancellation, prevent proxy buffering, and complete only on terminal provider event. Add backend contract tests that prove first delta arrives before completion and no source string is artificially split.

- **[OPEN — approval required] GAP-GPR-33: Resilient incremental client stream, safe real-time Markdown, and accessible status.** Extract the ad-hoc line parser from `ChatPanel.tsx` into a tested SSE parser/state machine; buffer React paints by animation frame without delaying/synthesizing provider deltas; render safe partial Markdown and citations as text arrives; distinguish pending/streaming/completed/error/cancelled; prevent unwanted autoscroll when the reader has moved away from bottom.

- **[OPEN — approval required] GAP-GPR-34: Fixed-corner AI composer redesign.** Redesign the form into a stable composer shell matching AI-chat interaction conventions: auto-growing textarea, reserved action rail, bottom-right anchored send button that never shifts with height, clear enabled/disabled/loading states, keyboard/IME behavior, touch target sizing, and consistent spacing.

- **[OPEN — approval required] GAP-GPR-35: Chat viewport fade and composer elevation.** Add a short theme-aware top/bottom scroll fade to the message-only viewport, use a progressive-enhancement mask with overlay fallback, preserve legibility/scrolling/accessibility, and add the requested text-input shadow/divider without clipping message controls or citations.

- **[OPEN — approval required] GAP-GPR-36: Sidebar and mobile navigation geometry.** Calculate and enforce the desktop left-panel minimum width from the search plus both action buttons and symmetrical margins; remove conflicting inline width caps; align search/control spacing to the shared spacing tokens/right panel; polish the mobile drawer icon, focus behavior, and full-width balanced control row.

- **[OPEN — approval required] GAP-GPR-37: Deterministic loading-mode continuity.** Make the load screen read only the last persisted, validated theme/language mode before the application becomes interactive, use the same design tokens as the destination UI, and test no light/dark or AR/EN flash occurs.

- **[OPEN — approval required] GAP-GPR-38: Verification, accessibility, and release readiness.** Add focused unit/contract/UI tests for each above gap; run backend regression and frontend production build; perform responsive visual/manual checks at desktop and mobile widths, keyboard and screen-reader smoke checks, provider-stream packet timing verification, and only then prepare a single reviewed branch/PR for user confirmation. No push until user approves.

- **[CLOSED] GAP-GPR-31: Credential incident containment and repository-history remediation.** Under explicit user authorization on 2026-07-22, documented provider/admin credentials were replaced with placeholders in current content, a fake secret-shaped test fixture was normalized, and all 8 reachable refs were surgically rewritten with `git-filter-repo --sensitive-data-removal`. Rewritten `main` was force-pushed and re-fetched cleanly. Verified no configured credential patterns across reachable history, 70/70 production-source files byte-identical before/after, workspace scan clean after reflog/object pruning, and GitHub Secret Scanning API returned zero alerts. Limitation: history cleanup cannot remove third-party clones/caches/chat transcripts; provider-side rotation remains required.

---

## 2026-07-22 — Current Implementation Gaps (fresh branch `feat/gpr-vault-streaming-ui-polish-20260722`)

- **[CLOSED] GAP-GPR-41: Test baseline repair and deterministic fixtures.** Repaired backend tests so they pass from a clean repository/DB using repo-relative fixtures, deterministic curated seeding, a small markdown upload fixture, JSON-token decoding in chat stream assertions, and updated graph expectations for the current 80-node production dataset. Verified with `PYTHONPATH=. pytest -q tests/` from `src/backend`: **16 passed, 1 warning in 35.99s**.
- **[CLOSED] GAP-GPR-42: Device-only encrypted server vault backend.** Added AES-256-GCM encrypted API-key vault backend, HttpOnly device cookie identity, `VaultProfileORM`, `/api/v1/vault/*` APIs, provider-check service, CORS origin hardening for cookie readiness, cryptography dependency, and vault tests. Verified with `GPR_VAULT_MASTER_KEY=<test-key> GPR_COOKIE_SECURE=false PYTHONPATH=. pytest -q tests/` from `src/backend`: **21 passed, 1 warning in 36.78s**. Secret scan found 0 configured findings.
- **[CLOSED] GAP-GPR-43: Frontend vault migration and Settings unification.** Converted frontend state to encrypted vault profile metadata, added one-time legacy raw-key localStorage migration into the server vault with raw-key deletion after success, unified SettingsModal opening through `AppContext`/`GlobalModals`, removed obsolete `ApiKeyModal.tsx`, changed ChatPanel to send `X-LLM-Profile-ID` instead of raw `X-LLM-API-Key`, and updated backend chat to decrypt the selected vault profile by HttpOnly device cookie. Verified backend suite: **21 passed, 1 warning in 36.13s**; frontend build: **compiled successfully**; secret scan: 0 findings.
- **[CLOSED] GAP-GPR-44: Old OTP/auth cleanup and dependency cleanup.** Replaced the last active raw-key test use with `/api/v1/vault/check-api`, updated SettingsModal to call it, removed unused OTP/login/session router/models/service/tests, removed auth router mounting/imports, and removed passlib/argon2 dependencies. Verified backend suite: **20 passed in 36.59s**; frontend build: **compiled successfully**; cleanup grep found no active auth/OTP references in source except stale README docs scheduled for GAP-GPR-49; secret scan: 0 findings.

- **[CLOSED] GAP-GPR-45A: Enriched JSON schema integration and data viewer compatibility.** Integrated Ahmed's new `deepseek_json_20260722_6a33e9.json` source format into the curated graph pipeline, preserving bilingual content, aliases/keywords, structured connections, role profiles, KPIs, answerable/not-answerable boundaries, approval status, verification date, and confidence metadata. Updated backend metadata storage/search/graph DTOs, graph API output, CitationDrawer enriched display, ObsidianGraphView bilingual/search behavior, and added round-trip tests. Verified backend suite: **21 passed in 36.38s**; frontend build: **compiled successfully**; secret scan: 0 findings.
- **[CLOSED] GAP-GPR-45: Prompt/control protocol hardening.** Added centralized versioned prompt builders, strict JSON navigation control parser, retrieved-context prompt-injection boundaries, citation/language/refusal rules, provider healthcheck prompt builder, ingestion prompt builder, online ReAct structured-control path, and prompt tests. Verified targeted prompt/chat tests: **7 passed in 1.18s**; full backend suite: **26 passed in 37.76s**; secret scan: 0 findings.
- **[CLOSED] GAP-GPR-46: True provider-delta backend streaming.** Added shared provider completion/streaming helpers, native Gemini `streamGenerateContent?alt=sse` parser, OpenAI-compatible delta forwarding, backend `delta` SSE events for real provider chunks, no-buffer SSE headers, and production provider-error events instead of local fallback. Added provider parser and stream header contract tests. Verified targeted streaming tests: **4 passed in 1.16s**; full backend suite: **28 passed in 36.85s**; secret scan: 0 findings.
- **[CLOSED] GAP-GPR-47: Frontend SSE parser and real-time delta rendering.** Added a robust SSE event-block parser supporting CRLF, comments, multiple data lines, final flush, and typed events; updated ChatPanel to consume `delta`/legacy `token` events, RAF-batch streaming paints without fake delay, preserve partial assistant content for final conversation storage, and surface backend error events. Verified frontend production build compiled successfully and secret scan found 0 findings. Abort/retry UX remains part of GAP-GPR-48 UI polish if a visible stop/retry control is added.
- **[CLOSED] GAP-GPR-48: Composer, fades, thinking spacing, sidebar/mobile/loading UI polish.** Implemented stable composer shell with bottom-right anchored send/stop button, chat viewport fade, composer shadow, balanced thinking-card spacing, full-width sidebar control row, mobile drawer state/ARIA/Escape/scroll-lock behavior, loading screen theme continuity through early theme script and shared tokens, stale status-dot CSS cleanup, and last-active graph camera focus. Verified frontend build: **compiled successfully**; backend regression: **28 passed in 37.07s**; secret scan: 0 findings.
- **[OPEN] GAP-GPR-49: Data/deployment/docs/repo hygiene cleanup.** Support Ahmed’s enriched JSON schema in backend/viewers/prompts, add/fix Railway config, remove or justify dead files, update README/roadmap, and clean generated config.
- **[OPEN] GAP-GPR-50: Final validation, secret scan, and release readiness.** Run full backend tests, frontend build, secret scans, manual UI checks, and prepare final merge-ready branch summary before main push approval.
